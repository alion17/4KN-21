#include 
int main(void)
{
   int n;
   printf("N:");
   scanf ("%i", &n);
   int k=0, temp=0;
   while (!(temp+k+1>n)) {
       ++k;
        temp+=k;
   }
   printf("K: %i\nSum:%i\n",k,temp);
   return 0;
}
